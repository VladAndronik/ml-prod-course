apiVersion: machinelearning.seldon.io/v1
kind: SeldonDeployment
metadata:
  name: week-5-seldon
  namespace: default
spec:
  annotations:
    seldon.io/rest-timeout: "60"
    seldon.io/grpc-timeout: "60"
  protocol: seldon
  predictors:
  - componentSpecs:
    - spec:
        containers:
        - image: 8092/app-seldon:latest
          name: classifier
          env:
          - name: GUNICORN_THREADS
            value: '1'
          - name: GUNICORN_WORKERS
            value: '1'
          - name: FLASK_SINGLE_THREADED
            value: '1'
          - name: SELDON_DEBUG
            value: '1'
          - name: WANDB_API_KEY
            value: "69d3861a93872cae14376c0b454a5223882050cb"
          imagePullPolicy: Always

    graph:
      name: classifier
      type: MODEL
      # parameters: [
      #   {
      #     "name": "model_load_path",
      #     "type": "STRING",
      #     "value": "model-artifacts/"
      #   }
      # ]
    name: model
    replicas: 1
